# 🚀 دليل الإعداد السريع - Werjo Bot

## ✅ ما تم إنجازه:

- ✅ تم تثبيت جميع المكتبات المطلوبة
- ✅ تم إعداد التوكن بنجاح
- ✅ البوت يتصل بـ Discord بنجاح
- ✅ اسم البوت: **Lovely Bot#0653**
- ✅ معرف البوت: **1500644674216394903**

## 🔧 الخطوات المتبقية:

### 1. دعوة البوت للسيرفر

استخدم هذا الرابط لدعوة البوت لسيرفرك:

```
https://discord.com/api/oauth2/authorize?client_id=1500644674216394903&permissions=2048&scope=bot
```

**أو قم بإنشاء الرابط يدوياً:**
1. اذهب إلى [Discord Developer Portal](https://discord.com/developers/applications)
2. اختر تطبيقك
3. اذهب إلى OAuth2 > URL Generator
4. اختر:
   - **Scopes:** `bot`
   - **Bot Permissions:** `Send Messages`, `Use Slash Commands`, `Embed Links`

### 2. تفعيل الصلاحيات المطلوبة (اختياري)

لتفعيل جميع مميزات البوت:
1. اذهب إلى [Discord Developer Portal](https://discord.com/developers/applications)
2. اختر تطبيقك
3. اذهب إلى **Bot** في القائمة الجانبية
4. في قسم **Privileged Gateway Intents**، فعّل:
   - ✅ **Message Content Intent**
   - ✅ **Server Members Intent** (اختياري)

### 3. الحصول على معرف القناة

1. في Discord، فعّل **Developer Mode**:
   - Settings > Advanced > Developer Mode ✅
2. انقر بالزر الأيمن على القناة التي تريد البوت يرسل فيها
3. اختر **Copy ID**
4. ضع المعرف في ملف `.env`:

```env
GENERAL_CHANNEL_ID=معرف_القناة_هنا
```

### 4. تشغيل البوت الكامل

بعد إكمال الخطوات أعلاه:

```bash
python bot.py
```

أو استخدم الملف المحسن:

```bash
python run.py
```

## 🧪 اختبار البوت

يمكنك اختبار البوت باستخدام:

```bash
python test_bot.py
```

هذا سيعرض معلومات الاتصال ومعرفات القنوات المتاحة.

## 📋 الأوامر المتاحة بعد التشغيل:

- `!صباح` - رسالة صباحية
- `!مساء` - رسالة مسائية
- `!حب` - رسالة محبة
- `!تشجيع` - رسالة تشجيع
- `!مساعدة` - قائمة الأوامر
- `!test` - اختبار البوت

## 🎯 المميزات التلقائية:

- 🌅 رسائل صباحية يومية (8:00 ص)
- 🌙 رسائل مسائية يومية (8:00 م)
- 💕 رسائل محبة عشوائية كل ساعتين
- 🎉 ترحيب بالأعضاء الجدد

## ❓ حل المشاكل الشائعة:

### البوت لا يرد على الأوامر:
- تأكد من تفعيل **Message Content Intent**
- تأكد من أن البوت لديه صلاحية الكتابة في القناة

### رسائل تلقائية لا تُرسل:
- تأكد من صحة `GENERAL_CHANNEL_ID` في ملف `.env`
- تأكد من أن البوت لديه صلاحية الكتابة في القناة

### البوت غير متصل:
- تأكد من صحة التوكن
- تأكد من اتصال الإنترنت

---

🎉 **Werjo Bot جاهز لنشر المحبة في سيرفرك!** 💕